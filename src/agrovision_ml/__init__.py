"""AgroVisión — pipeline de modelos.

Produce los cinco artefactos indivisibles que consume la app (§13.4):
`model.tflite`, `labels.json`, `calibration.json`, `metrics.json` y
`signature.bin`.
"""

__version__ = "1.0.0"
